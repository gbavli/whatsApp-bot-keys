# Temporary file for migration - add your Railway DATABASE_URL here
# DATABASE_URL=postgresql://username:password@host:port/database

# Example format:
# DATABASE_URL=postgresql://postgres:password@containers-us-west-123.railway.app:5432/railway

# Instructions:
# 1. Go to Railway dashboard -> Your project -> PostgreSQL service
# 2. Click Variables tab
# 3. Copy DATABASE_URL value
# 4. Replace the example above with your actual URL
# 5. Run: export DATABASE_URL=$(grep DATABASE_URL .env.migration | cut -d '=' -f2-)